Signed in as <%= current_user.name %> | <%= link_to "Logout", '/logout' %>
</br>
<%= link_to 'Buy a new film', new_buy_path %>
<table>
  <tr>
    <th>Film Title</th>
    <th colspan="3"></th>
  </tr>
 
  <% @buys.each do |buy| %>
    <tr>
      <td><%= buy.filmtitle %></td>
      <td><%= link_to 'Show', buy_path(buy) %></td>
    </tr>
  <% end %>
</table>

<%= link_to "Home Page", '/cool' %>