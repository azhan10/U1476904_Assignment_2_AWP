

<%= form_for @rental do |f| %>
 
  <% if @rental.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@rental.errors.count, "error") %> prohibited
        this rental from being saved:
      </h2>
      <ul>
        <% @rental.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  
  <%= f.collection_select :filmtitle, Film.order(:id),:filmtitle,:filmtitle, include_blank: true %>
 
  

  <%= f.select :filmduration, ['1 day', '2 days']%>

  <p>
    <%= f.label :rentalstatus %><br>
    <%= f.select :rentalstatus, ['Reserved', 'Rented Out', 'Retruned Late']%>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
 
<% end %>